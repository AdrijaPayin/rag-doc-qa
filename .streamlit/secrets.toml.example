# Copy this file to secrets.toml and set your deployed backend URL.
# This is only needed once your FastAPI backend is deployed (e.g. on Render).
# Locally, the app defaults to http://127.0.0.1:8000 if this isn't set.

API_URL = "https://your-app.onrender.com"
